gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Native libs/AARs baixadas por tool/android/fetch_native_libs.ps1 (P2 + F2,
# revisão de 2026-07-16) — binário não entra no git; o hash pinado no script
# é a proveniência. Rodar o script antes de cada build.
app/src/main/jniLibs/
app/libs/
