name: dubbing_engine
description: Core dubbing pipeline engine for OmniTranslator.
version: 0.1.0
environment:
  sdk: ^3.4.0
dependencies:
  sherpa_onnx: ^1.12.0
  path: ^1.9.0
  http: ^1.2.0
  crypto: ^3.0.0
  ffi: ^2.1.0
dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
  # Só para tool/check_native_libs.dart (lê o ZIP do APK/AAB). Vira dependência
  # de produção quando o ArchiveExtractor do Android (§5.3) for construído.
  archive: ^3.6.0
